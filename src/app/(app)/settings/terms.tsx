import { FeatureScreen } from "@/screens/feature-screen";
export default function Route() { return <FeatureScreen title="Terms and subscription terms" description="The production terms URL and localized subscription disclosures must be supplied before store submission." />; }
